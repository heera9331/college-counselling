"use client"
import { Header } from "@/components"
const RootLayout = (props: any) => {

    return (
        <>
            {props.children}
        </>
    )
}

export default RootLayout;